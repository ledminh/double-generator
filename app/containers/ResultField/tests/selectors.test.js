// import { fromJS } from 'immutable';
// import { makeSelectResultFieldDomain } from '../selectors';

// const selector = makeSelectResultFieldDomain();

describe('makeSelectResultFieldDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
